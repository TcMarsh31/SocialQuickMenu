<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="SocialQuickMenu.MainPage"
    Title="Floating Button"
   >
    <StackLayout>
    <ListView x:Name="listview" ItemTapped="listview_ItemTapped" SelectionMode="Single" HeightRequest="300" VerticalOptions="Start">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Label Padding="10" Text="{Binding Title}" HeightRequest="80"></Label>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

    <Image Source="logo.png" VerticalOptions="Center" HorizontalOptions="Center" Margin="0,-10,0,0"></Image>
        </StackLayout>
</ContentPage>